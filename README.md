🚗 ADAS System - Advanced Driver Assistance System
📋 О проекте
ADAS System - это профессиональная система компьютерного зрения для повышения безопасности автономного и полуавтономного вождения. Система в реальном времени анализирует дорожные сцены, обнаруживает потенциальные опасности и предоставляет водителю визуальные предупреждения.

🎯 Основные возможности
🔍 Обнаружение объектов: машины, пешеходы, светофоры, дорожные знаки

⚡ Реальное время: обработка изображений за менее чем 50 мс

🖥️ Веб-интерфейс: удобный drag-and-drop для загрузки изображений

📊 API: RESTful API для интеграции с другими системами

📈 Аналитика: статистика обнаружения и история запросов

🚀 Быстрый старт
Предварительные требования
Python 3.8 или выше

4 ГБ оперативной памяти

2 ГБ свободного места на диске


Установка за 3 шага
Шаг 1: Клонирование репозитория
bash

cd adas-system
Шаг 2: Установка зависимостей
bash
# Установите зависимости из requirements.txt
pip install -r requirements.txt

# Или установите вручную основные библиотеки:
pip install fastapi uvicorn ultralytics torch torchvision opencv-python pillow numpy
Шаг 3: Запуск системы
bash
cd api
python main.py
Проверка установки
После запуска откройте браузер и перейдите по адресу:

text
http://localhost:8000
Вы должны увидеть веб-интерфейс системы ADAS. Также доступна документация API:

text
http://localhost:8000/docs
📁 Структура проекта
text
ADAS-SYSTEM/
│
├── 📁 api/                          # Веб-сервер и интерфейс
│   ├── main.py                     # Главный серверный файл
│   ├── run.py                      # Скрипт для запуска сервера
│   └── requirements.txt            # Зависимости для сервера
│
├── 📁 src/                         # Исходный код системы
│   ├── detector.py                 * Основной детектор объектов
│   ├── hybrid_detector.py          * Гибридный детектор (расширенный)
│   └── simple_coco_detector.py     * Простой детектор
│
├── 📁 ADAS/                        # Обучение и конфигурация
│   ├── train_full.py               * Код обучения нейросети
│   ├── data/                       * Датасеты для обучения
│   └── README.md                   * Документация по обучению
│
├── 📁 static/                      # Статические файлы
│   └── (веб-интерфейс и ресурсы)
│
├── 📄 requirements.txt             # Основные зависимости
├── 📄 README.md                    # Эта документация
└── 📄 .gitignore                   # Игнорируемые файлы
🔧 Подробное описание ключевых файлов
api/main.py - Сердце системы
Этот файл содержит:

FastAPI приложение для веб-сервера

Все эндпоинты API (12+ endpoints)

HTML интерфейс с анимациями и drag-and-drop

Логику обработки загруженных изображений

Систему логирования всех запросов

Основные эндпоинты:

GET / - Главная страница с интерфейсом

POST /api/direct-predict/ - Загрузка и анализ изображения

GET /api/stats/ - Статистика работы системы

GET /health - Проверка работоспособности

GET /model-info - Информация о модели

src/detector.py - Ядро системы детекции
Этот файл содержит интеллектуальную часть системы:

python
class SimpleADASDetector:
    """
    Основной класс для обнаружения объектов на дороге.
    Использует нейросеть YOLOv8, дообученную на дорожных сценах.
    """
    
    def __init__(self):
        # Инициализация нейросети
        self.model = YOLO('yolov8s.pt')
        self.device = 'cuda' if доступен_GPU else 'cpu'
        
    def predict(self, image_path, confidence=0.25):
        """
        Анализирует изображение и возвращает обнаруженные объекты.
        
        Args:
            image_path: путь к изображению
            confidence: порог уверенности (0.1-0.9)
            
        Returns:
            Словарь с результатами детекции
        """
        # ... код детекции ...
Что он делает:

Загружает и предобрабатывает изображение

Запускает нейросеть для обнаружения объектов

Фильтрует результаты по порогу уверенности

Рисует bounding boxes (рамки) вокруг объектов

Сохраняет результат и возвращает данные

src/hybrid_detector.py - Расширенный детектор
Более продвинутая версия, которая:

Объединяет несколько моделей для повышения точности

Использует специализированные модели для разных типов объектов

Устраняет дубликаты детекций

Предоставляет цветовую маркировку по типу объекта

ADAS/train_full.py - Обучение модели
Файл для обучения собственной модели:

python
# Параметры обучения:
EPOCHS = 50           # Количество эпох обучения
BATCH_SIZE = 16       # Размер батча
IMG_SIZE = 640        # Размер изображений
LEARNING_RATE = 0.01  # Скорость обучения

# Запуск обучения:
model.train(
    data='dataset.yaml',
    epochs=EPOCHS,
    imgsz=IMG_SIZE,
    batch=BATCH_SIZE,
    lr0=LEARNING_RATE
)
⚙️ Настройка и конфигурация
Настройка порогов детекции
В файле src/detector.py вы можете настроить:

python
# Основные параметры (можно менять)
DEFAULT_CONFIDENCE = 0.25    # Порог уверенности по умолчанию
MIN_CONFIDENCE = 0.1         # Минимальный порог
MAX_CONFIDENCE = 0.9         # Максимальный порог
IMAGE_SIZE = 640             # Размер для обработки
MAX_DETECTIONS = 100         # Максимальное число объектов
Использование GPU
Система автоматически определяет доступность GPU. Если у вас есть NVIDIA GPU с CUDA, система использует её для ускорения.

Для принудительного использования CPU:

python
# В detector.py измените:
self.device = 'cpu'  # вместо автоматического определения
Настройка веб-сервера
В api/main.py:

python
# Параметры сервера
HOST = "127.0.0.1"    # Адрес сервера
PORT = 8000           # Порт
RELOAD = True         # Автоматическая перезагрузка при изменениях
LOG_LEVEL = "info"    # Уровень логирования
📡 API документация
Основные эндпоинты
1. Анализ изображения
text
POST /api/direct-predict/
Параметры:

file: файл изображения (jpg, png, jpeg)

confidence: число от 0.1 до 0.9 (опционально)

Ответ:

json
{
  "success": true,
  "detections": [
    {
      "class": "car",
      "confidence": 0.87,
      "bbox": [100, 150, 200, 250]
    }
  ],
  "count": 5,
  "processing_time_ms": 42.5,
  "result_image": "/static/results/result_123.jpg"
}
2. Статистика системы
text
GET /api/stats/
Ответ:

json
{
  "total_predictions": 150,
  "avg_time_ms": 45.2,
  "last_prediction": "2024-12-23T18:30:00",
  "model": "YOLOv8s Custom",
  "accuracy": "mAP50: 0.72"
}
3. Информация о модели
text
GET /model-info
Ответ:

json
{
  "name": "ADAS Custom Model v1.0",
  "description": "Модель для обнаружения объектов на дороге",
  "classes": ["car", "person", "traffic light", "sign"],
  "training_date": "2024-12-15",
  "performance": "mAP50: 0.72"
}
